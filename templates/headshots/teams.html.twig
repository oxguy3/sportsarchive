{% extends "base.html.twig" %}

{% block body %}
  <h1>Teams</h1>
  <ul>
    {% for team in teams %}
      <li><a href="/teams/{{ team.slug }}">{{ team.name }}</a></li>
    {% endfor %}
  </ul>
{% endblock %}
