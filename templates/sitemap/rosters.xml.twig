<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {% for roster in rosters %}
    <url>
      <loc>{{ url('roster_show', {slug: roster.team.slug, year: roster.year}) }}</loc>
      <changefreq>monthly</changefreq>
    </url>
  {% endfor %}
</urlset>
