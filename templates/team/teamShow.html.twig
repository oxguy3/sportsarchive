{% extends "base.html.twig" %}

{% block title %}{{ team.name }}{% endblock %}

{% block body %}
  <img src="https://sportsarchive-team-logos.s3.amazonaws.com/{{ team.slug }}.svg" class="float-md-end mb-2" style="height:200px" />
  <h1>{{ team.name }}</h1>
  {% if is_granted('ROLE_ADMIN') %}
    <a href="/teams/{{ team.slug }}/edit" class="btn btn-sm btn-primary mb-3">
      <i class="fas fa-pen"></i>
      Edit team
    </a>
  {% endif %}
  <div class="mb-2">
    {% if team.country %}
      <div>
        <img src="/images/flags/{{ team.country }}.svg" height="16">
        {{ team.countryName }}
      </div>
    {% endif %}
    {% if team.startYear or team.endYear %}
      <div>Played {{ team.startYear }}–{{ team.endYear }}</div>
    {% endif %}
    {% if team.website %}
      <div>Website: <a href="{{ team.website }}" target="_blank">{{ team.website }}</a></div>
    {% endif %}
  </div>
  <h2>Rosters</h2>
  {% if is_granted('ROLE_ADMIN') %}
    <a href="/teams/{{ team.slug }}/new-roster" class="btn btn-sm btn-success mb-3">
      <i class="fas fa-plus"></i>
      Create new roster
    </a>
  {% endif %}
  <ul>
    {% for roster in rosters %}
      <li>
        <a href="/teams/{{ team.slug }}/{{ roster.year }}">{{ roster.year }}</a>
        {% if is_granted('ROLE_ADMIN') %}
          <a href="/teams/{{ team.slug }}/{{ roster.year }}/edit-roster" title="Edit"><i class="fas fa-pen"></i></a>
        {% endif %}
      </li>
    {% else %}
      <p class="lead">None yet!</p>
    {% endfor %}
  </ul>
{% endblock %}
