{% extends "base.html.twig" %}

{% block title %}{{ team.name }} – add name{% endblock %}

{% block body %}
  <h1>
    <a href="/teams/{{ team.slug }}">{{ team.name }}</a>
    – add name
  </h1>
  {{ form(form) }}
{% endblock %}
